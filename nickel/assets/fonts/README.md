# Optional: bundled Cyrillic fonts for PDF export (offline / Windows without system fonts).
# Download DejaVu Sans from https://dejavu-fonts.github.io/ or use system fonts:
#   Windows: Arial / Segoe UI (auto-detected)
#   Linux/Docker: fonts-dejavu-core (installed in Dockerfile)
